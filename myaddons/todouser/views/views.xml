<odoo>
    <data>

        <!-- Inherit Form View to Modify it -->
        <record id="view_form_todo_task_inherited" model="ir.ui.view">
            <field name="name">Todo Task form - User extension</field>
            <field name="model">todo.task</field>
            <field name="inherit_id" ref="todotask.view_form_todo_task"/>
            <field name="arch" type="xml">

                <!-- 字段在同一视图出现多次，应使用XPath语法 -->
                <xpath expr="//field[@name='is_done']" position="before">
                    <field name="date_deadline"/>
                </xpath>

                <!-- 字段在视图出现一次 -->
                <field name="active" position="attributes">
                    <!-- invisible属性隐藏元素 -->
                    <attribute name="invisible">1</attribute>
                </field>

            </field>
        </record>

        <!-- explicit list view definition -->
        <!--
        <record model="ir.ui.view" id="todouser.list">
          <field name="name">todouser list</field>
          <field name="model">todouser.todouser</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="value"/>
              <field name="value2"/>
            </tree>
          </field>
        </record>
        -->

        <!-- actions opening views on models -->
        <!--
        <record model="ir.actions.act_window" id="todouser.action_window">
          <field name="name">todouser window</field>
          <field name="res_model">todouser.todouser</field>
          <field name="view_mode">tree,form</field>
        </record>
        -->

        <!-- server action to the one above -->
        <!--
        <record model="ir.actions.server" id="todouser.action_server">
          <field name="name">todouser server</field>
          <field name="model_id" ref="model_todouser_todouser"/>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": self._name,
            }
          </field>
        </record>
        -->

        <!-- Top menu item -->
        <!--
        <menuitem name="todouser" id="todouser.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
        <menuitem name="Menu 1" id="todouser.menu_1" parent="todouser.menu_root"/>
        <menuitem name="Menu 2" id="todouser.menu_2" parent="todouser.menu_root"/>
        -->
        <!-- actions -->
        <!--
        <menuitem name="List" id="todouser.menu_1_list" parent="todouser.menu_1"
                  action="todouser.action_window"/>
        <menuitem name="Server to list" id="todouser" parent="todouser.menu_2"
                  action="todouser.action_server"/>
        -->
    </data>
</odoo>